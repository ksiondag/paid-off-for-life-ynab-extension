import * as React from "react";
import { AppProps } from "../interfaces";

export default function Playback(props: React.PropsWithChildren<AppProps>) {
    return (
        <div className="Playback">
            Coming soon!
        </div>
    );
}